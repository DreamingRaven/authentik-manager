---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: akproviders.sso.goauthentik.io
spec:
  group: sso.goauthentik.io
  names:
    kind: AkProvider
    listKind: AkProviderList
    plural: akproviders
    singular: akprovider
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AkProvider is the Schema for the akproviders API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AkProviderSpec defines the desired state of AkProvider
            properties:
              consentFlow:
                description: ConsentFlow (requires) defines which authentik flow to
                  use to confirm users consent sharing data between applications
                type: string
              type:
                description: Type (required) of outpost from AppForwardAuth or DomainForwardAuth
                  for authentik app specific authentication or global domain authentication
                type: string
              url:
                description: URL (required) the url that should be launched when a
                  user selects this provider in the UI
                type: string
            type: object
          status:
            description: AkProviderStatus defines the observed state of AkProvider
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
