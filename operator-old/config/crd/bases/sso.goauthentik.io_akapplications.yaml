---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: akapplications.sso.goauthentik.io
spec:
  group: sso.goauthentik.io
  names:
    kind: AkApplication
    listKind: AkApplicationList
    plural: akapplications
    singular: akapplication
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AkApplication is the Schema for the akapplications API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AkApplicationSpec defines the desired state of AkApplication
            properties:
              applicationUI:
                description: ApplicationUI (optional) is additional settings that
                  define how this application appears in authentiks ui
                properties:
                  decription:
                    description: Description (optional) that describes the application
                      itself and what its purpose is
                    type: string
                  icon:
                    description: Icon (optional) url to an image to use in authentiks
                      dashboard
                    type: string
                  launchURL:
                    description: LaunchURL (optional) what url will be launched from
                      UI, defaults to providers url if empty
                    type: string
                  publisher:
                    description: Publisher (optional) name to identify who publishes
                      this application
                    type: string
                type: object
              group:
                description: Group (optional) is how authentik knows which applications
                  are to be grouped together
                type: string
              policyEngineMode:
                description: PolicyEngineMode (required) is whether any or all policies
                  must match to grant access
                type: string
              provider:
                description: Provider (required) is a reference to the AkProvider
                  CRD that this application should be related to (not the internal
                  authentik provider)
                type: string
              slug:
                description: Slug (required) is the internal name to be used in authentik
                  urls
                type: string
            type: object
          status:
            description: AkApplicationStatus defines the observed state of AkApplication
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
